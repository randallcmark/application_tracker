# Implementation Sequencing

This is the recommended order for the current resumable workstreams.

## Sequence

1. Harness adoption
   Stabilize the agent-facing routes, validator, and quality backlog before adding more planning surface.
2. Inbox follow-ons
   Clarify intake semantics and richer email/provider handling before background runtime work.
3. Job Workspace reduction
   Continue making the core execution surface calmer and more usable.
4. Artefact AI and competency evidence continuation
   Resume the active G4 grounding milestone after the workspace and intake surfaces are better framed.
5. Provider expansion
   Extend provider support after the visible-output contracts remain stable.
6. Scheduler/worker
   Add the smallest background runtime only after product inputs and outputs are clearer.
7. Admin/restore/ops
   Expand operational tooling and restore validation alongside the runtime story.
8. Deferred auth/provider modes
   Resume OIDC, proxy, or related auth/provider expansion only when higher-priority workflow work is stable.

## Notes

- Existing long-form plans remain the deep source docs for each workstream.
- Active execution plans are the preferred resumable entrypoint for future implementation sessions.
- Record sequencing changes in the relevant active plan and quality docs if priorities shift.
